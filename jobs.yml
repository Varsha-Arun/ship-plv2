jobs:
  - name: repo-manifest
    type: manifest
    steps:
      - GET: repo-img
    
  - name: repo-dcldeploy
    type: dclDeploy
    steps:
      - GET: repo-manifest
      - GET: repo-dclcluster
      
