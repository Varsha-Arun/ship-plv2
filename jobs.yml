jobs:
  - name: repo-manifest
    type: manifest
    steps:
      - GET: repo-image
    
  - name: repo-dcldeploy
    type: dclDeploy
    steps:
      - GET: repo-manifest
      - GET: repo-dclcluster
      
