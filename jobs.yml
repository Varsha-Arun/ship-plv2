jobs:
  - name: repo-manifest
    type: manifest
    steps:
      - GET: repo-img
      - GET: repo-param1
      - GET: repo-doption2
      - GET: repo-replica2
    
  - name: repo-dcldeploy
    type: dclDeploy
    steps:
      - GET: repo-manifest
      - GET: repo-dclcluster
      - GET: repo-param2
      - GET: repo-doption1
      - GET: repo-replica1
